# ─── App Info ─────────────────────────────────────────────────────
# The human-readable name of the application displayed in the UI
VITE_APP_NAME=orah

# ─── API Configuration ────────────────────────────────────────────
# The base URL of the backend API server that the frontend will communicate with
VITE_API_BASE_URL=http://localhost:3000
